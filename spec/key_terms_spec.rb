describe "Key Terms Challenge" do
  it 'is a text-only way of browsing and interacting with your computer' do
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_1.include?(s.downcase) }
  end

  it 'keeps track of the changes you make to a file' do
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_2.include?(s.downcase) }
  end

  it 'downloads onto your computer to keep track of what you do on its site' do
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_3.include?(s.downcase) }
  end

  it 'is the status code for “website not found”' do
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_4.include?(s.downcase) }
  end

  it 'is a computer that can be accessed through the Internet' do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_5.include?(s.downcase) }
  end

  it "is your computer's address on the internet" do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_6.include?(s.downcase) }
  end

  it 'keeps track of domain names and IP address on the internet' do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_7.include?(s.downcase) }
  end

  it 'is a way of designing a web page to be easily viewable on multiple screen sizes' do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_8.include?(s.downcase) }
  end

  it 'is the way web browsers describe colors' do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_9.include?(s.downcase) }
  end

  it 'is the secure way data is moved around the internet' do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_10.include?(s.downcase) }
  end

  it 'is the practice of sharing how a computer program was made and allowing anyone to customize the program as they see fit' do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_11.include?(s.downcase) }
  end

  it 'makes browser requests to the server asynchronous' do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_12.include?(s.downcase) }
  end

  it 'is the programming language of the browser' do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_13.include?(s.downcase) }
  end

  it 'holds all the data in your application' do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_14.include?(s.downcase) }
  end

  it "is the language to build a website's content" do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_15.include?(s.downcase) }
  end

  it "is the language to style a website's content" do 
    your_answer = __
    expect(your_answer).to satisfy {|s| answer_16.include?(s.downcase) }
  end

end